import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'firstmac-product-catalog',
  webDir: 'www'
};

export default config;
